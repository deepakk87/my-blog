{
  "name": "myblog",
  "version": "1.0.0",
  "description": "My Personal Blog Site",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "start": "node serve.js",
    "deploy": "npm run build && cd public && git init && git remote add origin  https://github.com/deepakk87/deepakk87.github.io.git && git add . && git commit -m 'deploy' && git push origin master --force"
  },
  "author": "Deepak Kumar",
  "license": "ISC",
  "dependencies": {
    "metalsmith": "^2.3.0",
    "metalsmith-markdown": "^1.3.0",
    "metalsmith-serve": "0.0.7",
    "metalsmith-watch": "^1.0.3"
  }
}
